
<div class="fos_user_user_show">

    <table class="table table-stripped" style="text-align: center">
        <thead >
        <th>{{ 'profile.show.username'|trans({}, 'FOSUserBundle') }}</th>
        <th>{{ 'profile.show.email'|trans({}, 'FOSUserBundle') }}</th>
        <th> Akcja</th>
        </thead>
        <tbody>
        <tr>
            <td>{{ user.username }}</td>
            <td>{{ user.email }}</td>
            <td><a href="/profile/show/edit">Edytuj profil</a></td>
        </tr> 
        </tbody>

    </table>

</div>
